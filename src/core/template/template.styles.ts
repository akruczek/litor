import { StyleSheet } from 'react-native'

export const templateStyles = StyleSheet.create({
  mainContainer: {
    width: '100%',
    height: '100%',
    backgroundColor: 'black',
    padding: 12,
  },
})

export const templateWatchStyles = StyleSheet.create({
  mainContainer: {
    width: '100%',
    height: '100%',
    backgroundColor: 'black',
  },
})
